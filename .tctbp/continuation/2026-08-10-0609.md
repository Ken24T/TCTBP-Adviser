# Handover Continuation — 2026-08-10

## Session Summary
A long, productive Phase-1 + Phase-2 session:

1. **TCTBP-Web (canonical) v0.3.5 + v0.3.6 shipped** — the two promote bugs found via the ddre-intranet-roadmap smoke test: (a) stale `promote.targets` shadowing the branch model (`buildEffectivePromoteTargets()`), (b) first-promote into a never-created target branch failing (`prepareTargetBranch` seeds from source when `allowFirstTargetPublish`). 82 tests green, clean on `main`, tag pushed.
2. **Adviser dogfood COMPLETED end-to-end** — the Adviser upgraded itself to canonical 0.3.6 on an auto-created upgrade branch, checkpointed, published, merged back to `main`, cleaned up the branch, and **shipped v0.2.14** (b9b251a). The entire self-sufficiency loop (auto upgrade-branch → apply → checkpoint → publish → merge guidance → gated cleanup → ship) validated on the real repo.
3. **Adviser branch realignment** — `development` and `review` were stale (missing v0.2.8–v0.2.13). Fast-forwarded both to `main` (clean, no force-push). Now development = review = main = b9b251a. Working branch is `development` going forward.
4. **UX: NextActionBar relocated** into the "Take action" section (first child, sticky) so the checkpoint/publish/merge next-action is visible exactly where the user applies Jasper's recommendations.
5. **NEW — Guarded merge actioner (Slice 3), UNCOMMITTED** — the merge step is now a button instead of terminal guidance (`mergeUpgradeBranch()`: auto-switch to destination, `git merge --ff-only`, push; refuses on dirty tree or divergence). **First real dogfood: Canasta Scoreboard fully merged + cleaned up via the app.**

Intentionally deferred: shipping the merge-actioner as an Adviser patch (v0.2.15); finishing audio-extractor + desklet-bandwidth-monitor (both parked at the merge step with the same upgrade branch); tctbp-adviser-bootstrap-test-repo smoke test (source 0.2.0).

## Plan Progress
- **Phase-1 consumers** — done & shipped: audio-extractor v0.4.6, rust-calendar v2.4.42, rust-clock v1.5.5, ddre-tim v0.3.1, canasta-scoreboard (re-upgraded to 0.3.6 today, merged + cleaned), media-seeker v0.1.1, desklet-bandwidth-monitor v1.5.2, ddre-intranet-roadmap v0.1.1.
- **Phase-2 (Adviser dogfood)** — Adviser itself upgraded to 0.3.6 + shipped v0.2.14. Next: ship the merge actioner patch, then continue using the app as a consumer.
- **In flight (next session):** audio-extractor + desklet-bandwidth-monitor both on `upgrade/tctbp-0.3.6-78b75fc` (published, at the merge step — one click each now); tctbp-adviser-bootstrap-test-repo mid-bootstrap on `upgrade/tctbp-bootstrap-47fe76a`.
- Plan reference: repo memory `/memories/repo/adviser-self-update-policy.md` (TWO-PHASE plan + full feature notes).

## Files Touched
The following are the **uncommitted merge-actioner changes** (19 files, working tree of `development`):

- `shared/tctbp-upgrade.ts` — added `TctbpMergeResult` (status/branch/destinationBranch/merged/pushed).
- `server/tctbp-source.ts` — `mergeUpgradeBranch()` actioner + `determineMergeDestination()` (deepest env branch the upgrade branch descends from; fallback workingBranch ?? productionBranch) + `branchIsAncestorOf()`.
- `server/api-upgrade-routes.ts` — `POST /api/repositories/:id/tctbp-merge` (requireEmptyBody → inspect → merge → portfolio.invalidate → 200).
- `server/http-errors.ts` — conflict codes `upgrade-merge-unavailable`, `upgrade-merge-blocked` (409).
- `src/api/upgrade.ts` + `src/api/index.ts` — `mergeTctbpUpgradeBranch(repositoryId)` client.
- `src/use-upgrade-apply.ts` — `mergeUpgradeBranch()` hook (confirm → POST → markMutated → refreshDetail + refreshUpgradePlan → feedback).
- `src/App.tsx`, `src/components/RepositoryDetail.tsx` — wired `onMergeUpgradeBranch` through to bar + panel.
- `src/components/NextActionBar.tsx` — merge case in `runJourneyStep` + `journeyButtonLabel` ("Merge upgrade branch"/"Merging…"), removed the "Refresh after merging" guidance path, hourglass `disabled:!cursor-wait` + container `cursor-wait` while busy.
- `src/components/TctbpUpgradePanel.tsx` — new "Upgrade branch merge" section (button when `cleanup.branch && !cleanup.available`).
- `src/upgrade-journey.ts` — merge guidance now names the **working branch** (`workingBranch ?? productionBranch`) instead of production-first.
- Tests: `server/tctbp-source.test.ts` (+5 merge tests), `server/api.test.ts` (+1 route 409), `src/api/api-client.test.ts` (+1), `src/upgrade-journey.test.ts` (updated to working-branch), `src/next-action-bar.test.tsx` (+1 merge button), `src/tctbp-upgrade-panel.test.tsx` (+1 merge section), `src/repository-detail.test.tsx` (prop).

Earlier committed work (in v0.2.14 / b9b251a): NextActionBar + journey/next-action resolvers, auto upgrade-branch + gated cleanup, hourglass, preview-button gating, checkbox disable.

## Tickets
No ticket system in play. Open items for next session (informal):
- Ship the uncommitted merge actioner as Adviser v0.2.15 (checkpoint → publish → promote development→review→main → ship).
- Finish audio-extractor + desklet-bandwidth-monitor merges (one click each in the app).
- tctbp-adviser-bootstrap-test-repo smoke test (last Phase-1 consumer, source 0.2.0).

## Checkpoint Log
- `68b17db` checkpoint: preserve local working state (self-sufficiency + UX feature set) — published to origin/upgrade/tctbp-0.3.6-78b75fc.
- `bd4e5ec` checkpoint: preserve local working state (final pre-merge commit on the upgrade branch).
- `b9b251a` chore(release): v0.2.14 — Adviser ship (tag v0.2.14 pushed, package.json 0.2.14).
- After b9b251a: `development`/`review` fast-forwarded to b9b251a and pushed (realignment). The merge actioner is uncommitted on top.

## Mistakes & Gotchas
- **"Switch back to master" was NOT a bug** — the browser was showing Audio Extractor (its config legitimately uses `master`), not TCTBP-Adviser. Upgrade branch names are identical across consumers (`upgrade/tctbp-0.3.6-78b75fc` derives from the canonical revision), which makes repos easy to confuse. **Always check the h1 before debugging what a card/bar shows.**
- **Merge guidance destination was wrong for long-lived repos** — old code used `productionBranch ?? workingBranch`, saying "main" even when the repo works on development. Fixed in the actioner (deepest-ancestor env branch, fallback working first) and the journey text.
- **Development/review were stale** (missing 6 releases). Realigned by fast-forward (both were ancestors of main → clean, no force-push). Don't assume the config's branch model matches reality — check `git log` before trusting branch guidance.
- **Pre-existing (unfixed):** header nav tooltips (opacity-0) extend past the viewport → ~52px horizontal scrollWidth overflow on every page. Out of scope.

## Branch & Commit Context
- Machine: ARAGORN
- Branch: `development` (working branch of the long-lived model; development → review → main)
- Last commit: `b9b251a` (tag v0.2.14) — `git log -1`
- Working tree: **19 modified files (uncommitted merge actioner)** — MUST be checkpointed before continuing
- Upstream: in sync with `origin/development`
- Remote: https://github.com/Ken24T/TCTBP-Adviser.git
- Other repos: audio-extractor + desklet-bandwidth-monitor each on `upgrade/tctbp-0.3.6-78b75fc` (published, clean); tctbp-adviser-bootstrap-test-repo on `upgrade/tctbp-bootstrap-47fe76a`

## Next Session
1. **Checkpoint the merge-actioner working tree** (19 modified files) to preserve it, then publish. (Or invoke the handover's resume flow which restores this state.)
2. Promote `development` → `review` → `main` and ship the Adviser **v0.2.15** (the guarded merge actioner patch).
3. In the app, finish **audio-extractor** and **desklet-bandwidth-monitor** — each is at the merge step: click "Merge upgrade branch", then cleanup.
4. Run the **tctbp-adviser-bootstrap-test-repo** smoke test (last Phase-1 consumer, source 0.2.0).
